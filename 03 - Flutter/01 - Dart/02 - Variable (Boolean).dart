void main(List<String> args) {
  bool status = true; // A variable that only can accept "true" or "false".
  print(status);      // Output: true

  status = false; // Modify the value in "status".
  print(status);  // Output: false
}