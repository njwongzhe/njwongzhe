# Fundamental Flutter Development

- There is some of the file and folder has been ignored by git, such as "build", "test" and ".dart_tool".
- You can find the ignored files and folders in the ".gitignore" file.
- Thus, it should not exist in this github repository.